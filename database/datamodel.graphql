type Link {
  id: ID! @unique
  createdAt: DateTime!
  description: String!
  url: String!
}

type User {
  id: ID! @unique
  name: String!
  phones: [Phone!]!
}

type Phone {
  id: ID! @unique
  number: String!
}
